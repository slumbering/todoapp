name: todoapp
sources:
  - path: workflows/build
    sdk: ts
  - path: workflows/build-easy
    sdk: ts
  - path: workflows/deploy
    sdk: go
  - path: workflows/deploy-easy
    sdk: ts
dependencies:
  # Erik's modified yarn extension
  - git:
      remote: git@github.com:sipsma/cloak.git
      ref: workflow-clean
      path: examples/yarn/cloak.yaml
  # Erik's modified netlify extension
  - git:
      remote: git@github.com:sipsma/cloak.git
      ref: workflow-clean
      path: examples/netlify/go/cloak.yaml
